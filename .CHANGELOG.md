# Dynora CarDealer Changelog

## Update 0.5.1 - 2026-03-23

### cl_tuning.lua (Tuning UI + Color Picker rework)
1. LVS Performance tuning item prices drastically reduced (turbo from 2500->250, compressor 3000->300, racingTires 1500->150, exhaust 800->80, gauge 500->50, manualTransmission 1000->100).
2. `OpenColorPicker` layout adjusted: frame size changed from 450x400 to 450x380; mixer resized to 360x200; preview box resized to 325x40 and repositioned; OK/Cancel button Y position from 340->325.
3. `OpenColorPicker` drawing updates: accent outline changed to half width and no thickness; header paint definition corrected; preview outline changed to 0 thickness.
4. Color picker close button removed (not present in current function body).
5. Updated `PANEL` comments and UI details for neon/body color sections in tuning panel (more spacing and labels).
6. No functional change to pricing engine in `UpdatePrice` (still has full original item prices, but UI list uses reduced values).

### Notes
- This changelog entry is based on the current working diff in `lua/cardealer/cl_tuning.lua` in the repository.

